map $http_x_forwarded_proto $fusion_forwarded_proto {
    default $http_x_forwarded_proto;
    "" $scheme;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 100m;

    # 运行时部署配置不可缓存，避免容器重建后浏览器继续使用旧 API 地址。
    location = /runtime-config.js {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $fusion_forwarded_proto;
        add_header Cache-Control "no-store, max-age=0" always;
    }

    # ---------- 前端 ----------
    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $fusion_forwarded_proto;

        # Next.js HMR (开发时用到)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # ---------- 后端 API ----------
    # /api/xxx  →  backend:18080/api/xxx
    location /api/ {
        proxy_pass http://backend:18080;
        proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $fusion_forwarded_proto;

        # SSE 流式输出支持
        proxy_buffering off;
        proxy_cache off;
        # 视频生成/ComfyUI 试运行是同步长请求（后端最长 120 分钟），超时必须对齐
        proxy_read_timeout 7500s;

        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # ---------- 后端媒体资源 ----------
    location /media/ {
        proxy_pass http://backend:18080;
        proxy_http_version 1.1;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
